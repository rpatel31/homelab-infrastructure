# https://grafana.com/docs/grafana/latest/datasources/prometheus/configure/
apiVersion: 1

datasources:
    - name: Prometheus 
      uid: prometheus
      type: prometheus
      access: proxy
      url: "{{ grafana_prometheus_url }}"
      isDefault: true
      editable: false